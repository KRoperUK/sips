# Production Environment Configuration for sips.kroper.uk
# Copy this file to .env.production for production deployment

# Google OAuth
# Callback URL: https://sips.kroper.uk/api/auth/callback/google
# Get credentials from: https://console.cloud.google.com/
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret

# Apple OAuth
# Callback URL: https://sips.kroper.uk/api/auth/callback/apple
# See APPLE_SIGNIN_SETUP.md for setup instructions
# Get credentials from: https://developer.apple.com/
APPLE_CLIENT_ID=uk.kroper.sips.web
APPLE_CLIENT_SECRET=your_apple_jwt_token

# NextAuth Configuration
# IMPORTANT: Must match your production domain
NEXTAUTH_URL=https://sips.kroper.uk

# Generate with: openssl rand -base64 32
# CRITICAL: Use a different secret than development!
NEXTAUTH_SECRET=your_production_nextauth_secret

# Trust the proxy (nginx, Caddy, etc.) for X-Forwarded-* headers
# Required when running behind a reverse proxy with HTTPS
AUTH_TRUST_HOST=true

# Node Environment
NODE_ENV=production

# Production Deployment Checklist:
# ✓ 1. Update Google OAuth Authorized redirect URIs:
#      https://sips.kroper.uk/api/auth/callback/google
# ✓ 2. Update Apple Sign In Return URLs:
#      https://sips.kroper.uk/api/auth/callback/apple
# ✓ 3. Generate NEW NEXTAUTH_SECRET for production
# ✓ 4. Ensure NEXTAUTH_URL matches your domain exactly
# ✓ 5. Update docker-compose.yml or deployment config
